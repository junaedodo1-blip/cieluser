name: Daily Instagram Carousel Automation (project\ciel)

on:
  schedule:
    # Runs daily at 10:00 AM UTC (4:00 PM BST / 6:00 AM EST)
    - cron: '0 10 * * *'
  workflow_dispatch:
    inputs:
      topic:
        description: 'Custom Carousel Topic (optional)'
        required: false
        default: ''
      hook_angle:
        description: 'Specific Hook Angle (optional: contrarian_truth, case_study_directing, technical_formula, before_vs_after, step_by_step_anatomy, curiosity_juxtaposition, commerce_conversion)'
        required: false
        default: ''
      force_strict_brand:
        description: 'Force Strict project\\ciel Brand Day'
        type: boolean
        default: false
      dynamic:
        description: 'Use Dynamic Performance-Weighted Selection'
        type: boolean
        default: true

permissions:
  contents: write

jobs:
  run-daily-carousel:
    name: Generate & Dispatch Daily Carousel
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🟢 Setup Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10.33.2

      - name: 📦 Install Dependencies
        run: pnpm install --frozen-lockfile || pnpm install

      - name: 🚀 Run Daily Carousel Pipeline
        id: run_pipeline
        run: |
          ARGS=""
          if [ "${{ github.event_name }}" = "schedule" ] || [ "${{ inputs.dynamic }}" = "true" ]; then
            ARGS="$ARGS --dynamic"
          fi
          if [ "${{ inputs.force_strict_brand }}" = "true" ]; then
            ARGS="$ARGS --force-strict-brand"
          fi
          if [ -n "${{ inputs.topic }}" ]; then
            ARGS="$ARGS --topic-key '${{ inputs.topic }}'"
          fi
          
          node --import tsx/esm scripts/carousel_automation/daily_runner.ts $ARGS
        env:
          BUFFER_ACCESS_TOKEN: ${{ secrets.BUFFER_ACCESS_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: 🚀 Publish & Schedule to Buffer
        run: |
          node --import tsx/esm scripts/carousel_automation/publish_buffer.ts
        env:
          BUFFER_ACCESS_TOKEN: ${{ secrets.BUFFER_ACCESS_TOKEN }}

      - name: 💾 Commit & Push Automation State
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add carousel_automation_state.json pending_notification_posts.json .cheat-state.json scripts/ predictions/
          git add -f out/
          git commit -m "chore(automation): update carousel state, drafts, and predictions [skip ci]" || echo "No changes to commit"
          git push origin HEAD:${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
